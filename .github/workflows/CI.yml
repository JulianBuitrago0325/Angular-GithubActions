name: Integración continua angular

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  push_to_master:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-node@v1
      with:
        node-version: '12.x'
    - run: npm install
    - run: npm run build
    - uses: docker/build-push-action@v1
      with:
        username: julian0122
        password: JcBc2040
        repository: angular/project-julian
        tags: latest
